name: couldai_user_app
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  
  # UI Icons
  cupertino_icons: ^1.0.8
  
  # Camera handling (Official plugin supports macOS)
  camera: ^0.11.0+2
  
  # Database (SQLite for Desktop)
  sqflite_common_ffi: ^2.3.3
  sqlite3_flutter_libs: ^0.5.20
  
  # File path management
  path_provider: ^2.1.2
  path: ^1.9.0
  
  # Date formatting
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
